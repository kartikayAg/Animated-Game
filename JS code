var counter = 0;

function increment() {
    counter++;
    document.getElementById("counterValue").textContent = counter;
}

function decrement() {
    counter--;
    document.getElementById("counterValue").textContent = counter;
}
